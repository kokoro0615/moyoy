# Open questions and blockers

Status: Gate C measurement questions are resolved; remaining items affect Bootstrap,
Gate A/D/E, or human release approval as noted.

Annotation-linked questions below use stable IDs from the
[design annotation ledger](design-annotation-ledger.md). The safe default is not an
approval and does not convert qualitative notes into measurable values.

| ID   | Decision needed                                                                                              | Owner                        | Gate impact  | Safe default                                         |
| ---- | ------------------------------------------------------------------------------------------------------------ | ---------------------------- | ------------ | ---------------------------------------------------- |
| Q-01 | **RESOLVED FOR CURRENT DESIGN 2026-08-18:** current design copy, including displayed product/footer content, approved for Web use; do not invent or silently alter it | Client/content owner         | Gate A       | use the private approved-copy ledger verbatim        |
| Q-02 | **RESOLVED 2026-08-19:** omit the entire NEWS feature from the initial implementation. The apparent second NEWS instruction was a mis-binding: source re-measurement shows the 「この二つのコンテンツは一旦非表示で公開する」 leaders land on the two footer legal links, not on NEWS content, so no conflict exists | Product owner | Gate A/C | no NEWS DOM, links, placeholder copy, or imagery; footer links hidden at launch |
| Q-03 | **RESOLVED 2026-08-19:** retain the approved viewport-fixed visual; native modal dialog, explicit 44×44 close, Escape, focus trap/restore, deterministic background inertness, and exact scroll restoration form the WCAG baseline. Outside click and unapproved destinations remain absent | Accessibility/technical owner | Gate E | implement and manually verify the recorded contract |
| Q-04 | **RESOLVED FOR CURRENT SCOPE 2026-08-18:** Web use confirmed for the four chapter photos, logo, contour lines, product drawings, and current design copy. NEWS assets are excluded with NEWS | Rights owner                 | Gate A/E     | generate only approved derivatives; do not ship private masters |
| Q-05 | **REOPENED 2026-08-19:** the source names Futura PT and 秀英角ゴシック銀 Std and supplies the Adobe Fonts kit `yhj3ndj` that delivers them, so the licensed delivery path was specified all along and is now shipped. Two items remain: register the deploy origin in the Adobe Fonts web project (an unregistered origin gets 412 and falls back), and confirm the Japanese family's web slug against the kit stylesheet. Jost Variable and Noto Sans JP Variable stay as the measured OFL fallbacks | Client/design owner | Gate E | register the origin, confirm the slug, keep the OFL fallbacks pinned |
| Q-06 | **IMPLEMENTED CANDIDATE 2026-08-19:** 640 px content-stress breakpoint; 390 uses SP reflow with 24 px gutters and uniform `viewport/751` media scaling, 768 uses 48 px tablet insets and three product columns. No page or screenshot stretch | Designer/technical owner | Gate D/E | fresh exact-frame visual approval remains required |
| Q-07 | **IMPLEMENTED CANDIDATE 2026-08-19:** scale the complete PC artboard, including all chapter and decorative imagery, uniformly to full viewport width; 2560 px selects source-derived `pc-2560-*` derivatives and never enlarges a 2400 px derivative | Designer | Gate C/D/E | exact wide-frame visual approval remains required |
| Q-08 | **IMPLEMENTED CANDIDATE / UNVERIFIED 2026-08-19:** all four annotated motions exist as one measured client island plus CSS — contour lag (8 %, hero/product/footer, viewport-space caps with a compact set below 640 px), decorative object parallax (eight objects, unequal 8–30 px caps), the 3.2 s repeating scroll line, and fixed chapter photography. The photograph is now genuinely fixed: it is sized to the viewport and translated by the whole scrolled distance, measured at 0 px of screen movement across 800 px of scroll (previously 99.2 % of page speed). Ratios, caps and duration remain implementation judgment | Designer/accessibility owner | Gate D/E | normal/reduced browser evidence exists at 1200/375/1440/390/768/2560; keep release blocked until the selected ratios, caps and duration are approved |
| Q-09 | **DEFERRED by owner on 2026-08-18:** define fragrance and legal/shop/social link destinations and missing-route behavior; no destination is specified by the pink annotations | Product owner                | Gate A/D     | skip destination wiring and render no dead links     |
| Q-10 | Confirm supported Safari/iOS versions; is Safari earlier than 16.4 required?                                 | Technical owner              | Bootstrap    | do not assume Tailwind 4 compatibility               |
| Q-11 | Confirm Vercel team/scope, project, commercial plan, protected Preview, and intended hostname                | Deployment owner             | Gate E       | do not deploy or change aliases                      |
| Q-12 | Approve analytics/consent/Speed Insights and privacy disclosures                                             | Product/legal owner          | Gate A/E     | no analytics                                         |
| Q-13 | Approve metadata, canonical host, robots policy, social image, and structured data                           | Content/SEO owner            | Gate A/E     | Preview noindex; production blocked                  |
| Q-14 | Confirm whether a form, CMS, or localization is in scope                                                     | Product owner                | Architecture | static content only                                  |
| Q-15 | **PARTIALLY RESOLVED 2026-08-19:** detached Ed25519 signature evidence is selected and the evidence work is authorized. A named approver ID and reviewed public key still must be enrolled in the tracked signer policy; chat approval is not the detached signature | Client/technical owner | Gate E | technical-candidate preflight remains blocked; never generate or infer the approver's key |
| Q-16 | **RESOLVED 2026-08-19:** SP menu remains at top; dialog is viewport-fixed with safe-area padding, focus containment/return, inert background, Escape, and explicit close. **Amended 2026-08-21 (VF-47):** the drawer is no longer in the top layer and the scroll lock is no longer on `<body>` — both made the drawer a Safari 26 browser-bar colour source while open — so modality now rests entirely on the shell `inert` plus the focus trap. **Amended 2026-08-21 (VF-48/VF-49):** the scroll lock is no longer a layout one at all — a fixed page clips `.chapter-photo-mirror` out of the browser-bar strips and stops the root scroller driving it — and focus entry and return now state their own modality with `focus({ focusVisible })`, because WebKit shows the focus ring on a programmatic focus whatever the reader did | Accessibility owner | Gate E | verify across required browsers and assistive technology |
| Q-17 | **RESOLVED AS CONTRACT 2026-08-19:** semantic selectors, chapter/footer anchor strategies, modal setup/state attributes, symmetric detector, region masks, result IDs, and predeclared thresholds are recorded in `fidelity.config.json` | Technical owner | Gate D/E | exact-frame references and executed PASS evidence still required |
| Q-18 | **RESOLVED FOR CURRENT ENVIRONMENT 2026-08-19:** Playwright WebKit host dependencies are installed and a normal WebKit foundation run passes 5/5. This is local execution evidence, not supported-Safari product policy | Technical owner | Bootstrap/E | retain WebKit in the three-engine gates; Q-10 still defines support policy |
| Q-19 | **TECHNICALLY REVIEWED 2026-08-19:** the production candidate passes three-engine keyboard modal, 1440/768/390 plus 200%/400% reflow, target-size, structure, and axe checks. Photo-text contrast required a dark glyph halo; the modal uses solid `#68702f` because the approved 217/255 surface yields only 4.08:1 for white over paper. Named human/AT review and visual approval of both deviations remain required | Accessibility/design owner | Gate E | keep release blocked; see `docs/manual-wcag-review.md` |
| Q-20 | **CORRECTED CANDIDATE / PARTLY UNVERIFIED 2026-08-19:** menu bounds and PC/SP hero/footer contour geometry were remapped from source landmarks; rendered comparison isolated CSS opacity and scale/offset errors; contour motion is implemented | Designer/technical owner | Gate D/E | use `docs/visual-fidelity-defects.md`; 1200/375 evidence awaits human review and 1440/768/390 remain unverified without approved exact frames |
| Q-21 | **RESOLVED BY OWNER APPROVAL 2026-08-19:** the footer contact is literal approved production copy. `config/public-copy-allowlist.json` binds the exact UTF-8 values and source path by SHA-256; the detector remains active for every other address or telephone occurrence | Technical/content owner | Gate E | retain the hash-bound exception and review the complete diff before publication |
| Q-22 | **RESOLVED BY OWNER APPROVAL 2026-08-19:** production assets and client-containing visual regression frames have distinct `approved-production-asset` and `approved-production-baseline` classes. The exact path/hash/media-type allowlist covers 51 assets and 21 viewport baselines; no client material is relabeled synthetic and every file remains below the 10 MiB cap | Rights/technical owner | Gate D/E | keep staged policy verification and human rights review required |
| Q-23 | **NEW 2026-08-19:** DA-MEDIA-01 asks for fixed chapter photography. The approved derivatives are exact silhouette crops with no RGB outside the alpha, so a literal viewport pin is impossible without new derivatives carrying vertical bleed. The candidate ships a bounded downward drift inside a pinned silhouette. Approve either the bounded interpretation or new bleed derivatives | Designer/rights owner | Gate D/E | keep the bounded drift; do not stretch or re-crop an approved derivative |
| Q-24 | **RESOLVED CANDIDATE BY OWNER APPROVAL 2026-08-19:** each chapter now uses a separately hashed lightweight alpha-only PC/SP WebP mask, while the RGB chapter image remains a lazy `<picture>` resource. Initial Chromium inspection confirms DUSK/DAWN/ALPINE RGB requests are absent before scroll | Technical/rights owner | Gate D/E | retain the resource regression and mask provenance checks |
| Q-25 | **IMPLEMENTED CANDIDATE 2026-08-19:** SP chapter geometry derives heights and source-origin steps from `--sp-source-scale: viewport/751` across 375–639 px. Chromium regression covers 390 and 639; exact external 390 reference approval remains unavailable | Designer/technical owner | Gate D/E | do not close visual fidelity without the approved exact 390 frame |
| Q-23b | **NEW 2026-08-21:** the browser-bar mirror makes DA-MEDIA-01's full bleed real, but the approved SP derivative is 927.7 CSS px tall against an 874 px screen, so covering both strips leaves 53.7 px of travel instead of 213.7 and starts the frame about a status bar above the window. Approve the reduced travel and the shifted crop origin, or authorise derivatives carrying vertical context beyond the approved silhouette | Designer/rights owner | Gate D/E | keep the reduced travel; do not stretch or re-crop an approved derivative |
| Q-27 | **NEW 2026-08-21:** the top browser-bar strip could not be measured from the first device recording — 63 CSS px carries five code cells and the correlation peak was separated from its nearest rival by as little as 0.015. The bottom strip, where the reported defect is, measured 0.05–0.64 CSS px. Decide whether the top edge needs its own measured evidence before release or whether visual confirmation on all four photographs is sufficient | Technical/design owner | Gate D/E | report the top edge as `UNVERIFIED` |
| Q-26 | **IMPLEMENTED CANDIDATE 2026-08-19:** fidelity coverage is 21 viewport/state frames: top, four chapters, footer, and menu-open at 1440×900, 768×1024, and 390×844. The old fullPage visual test was replaced with viewport captures | Technical owner | Gate D/E | keep exact references and executed PASS evidence separate from regression goldens |
| Q-28 | **NEW 2026-08-21:** VF-46 corrected the SP `menu` label, and the same measurement shows the **PC** label carries the identical defect, uncorrected and out of scope for that fix. Measured out of the approved `pc-reference.svg`: the word's ink runs x `1111.29–1141.80` (**30.51 px**, x-height 6.05), which in the shipping face needs about **13 px with 0.66 px of tracking**; it ships at 10 px and renders **26.1 px** of ink, 14 % short, with the baseline 0.55 px high. It reads as a size error rather than a position error because the PC reference sets the word beside the mark, so `left: 0` is the correct anchor there and the shortfall does not move it. Correcting it changes the approved PC page-top frame and the 1200 px fidelity figures, so it is left for a decision rather than taken. Approve the correction to 13 px / 0.66 px with `top` restated to 7.22 px, or accept the shipped 10 px as the PC appearance | Designer/technical owner | Gate D/E | leave the PC control at 10 px and keep this recorded as an unresolved fidelity diff |

Resolution protocol:

1. Record the answer in both DESIGN ledgers.
2. Update the invariant ledger and measured implementation specification.
3. Update asset provenance when rights or delivery changes.
4. Close a Gate only when all required evidence is present; do not reinterpret a safe
   default as approval.
